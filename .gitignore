# Cloned by init.sh (external repo — not vendored here)
/MulTaBench/

# Machine-specific VM bootstrap / credential-sync helper
/sync.sh

# Virtualenvs
.venv/
venv/

# Large OSM extracts (1.6G) — kept local, never committed
/data/

# Dataset CSVs (raw + generated) — shipped to the GPU box via scripts/sync.sh,
# not git. Too big / churny to track; code reproduces them from data/ + the LLM.
/results/
/*.csv
# Generated pipeline outputs (enriched/described CSVs, batch json, logs, catboost)
/artifacts/
# Prompt-screening intermediates (the committed df2_*.csv + results.txt stay)
/experiments/screen/_sample_enriched.csv
/experiments/screen/*.described.csv
/experiments/datasets-analysis/
# Logs / caches
*.log
wandb/
__pycache__/
*.pyc
.env

# Python tooling, test, coverage, and build artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
build/
dist/
*.egg-info/
.ipynb_checkpoints/

# Model training outputs (CatBoost includes TensorBoard event files)
catboost_info/

# Local environment variants; retain committed example templates
.env.*
!.env.example


logs/
paper/
