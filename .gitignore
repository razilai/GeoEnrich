# Cloned by init.sh (external repo — not vendored here)
/MulTaBench/

# Virtualenvs
.venv/
venv/

# Large OSM extracts (1.6G) — kept local, never committed
/data/

# Dataset CSVs (raw + generated) — shipped to the GPU box via sync.sh, not git.
# Too big / churny to track; code reproduces them from data/ + the LLM.
/results/
/*.csv

# Logs / caches
*.log
wandb/
__pycache__/
*.pyc
.env
