# Extra dataset-build deps, installed by init.sh INTO MulTaBench/.venv (the
# single execution environment). pandas/numpy/python-dotenv already come from
# MulTaBench's own requirements, so do not list them here and trigger a second
# resolution or upgrade.
# Stage 0: raw-scrape cleaning (airbnb_surroundings/clean.py)
pyspark
# Stage 1: Overture Places POI enrichment (airbnb_surroundings/build.py)
geopandas
duckdb
rapidfuzz
# Stage 2: LLM surroundings summary (airbnb/describe.py)
pydantic-ai
