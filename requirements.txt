# Dataset-build deps, installed by init.sh INTO MulTaBench/.venv (single shared env).
# pandas/numpy come from MulTaBench's own requirements — not pinned here so uv
# doesn't re-resolve MulTaBench's CUDA torch build.
# Stage 0: raw-scrape cleaning (airbnb_surroundings/clean.py)
pyspark
# Stage 1: Overture Places POI enrichment (airbnb_surroundings/build.py)
geopandas
duckdb
# Stage 2: LLM surroundings summary (airbnb/describe.py)
pydantic-ai
python-dotenv
